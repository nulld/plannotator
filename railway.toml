[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/paste-service/Dockerfile"

[deploy]
startCommand = "bun run apps/paste-service/targets/railway.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
